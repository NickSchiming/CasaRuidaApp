<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CasaRuidaApp.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="logo_spotify.png"
                HeightRequest="120"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Visualizador de Músicas"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Button
                x:Name="ConnectBtn"
                Text="Conectar" 
                Clicked="OnConnectClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="ConnectOtherBtn"
                Text="Conectar Outro Spotify" 
                Clicked="OnConnectOtherClicked"
                HorizontalOptions="Fill" />

            <Label
            x:Name="OutputLabel"
            Text=""
            Style="{StaticResource SubHeadline}"
            SemanticProperties.HeadingLevel="Level2"
            SemanticProperties.Description="Output label" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
